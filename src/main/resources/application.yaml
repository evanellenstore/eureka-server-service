spring:
  application:
    name: eureka-server
  config:
    import: optional:configserver:http://localhost:8871

server:
  port: 8872

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
